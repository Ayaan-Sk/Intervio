'use server';

/**
 * @fileOverview This file is no longer used for question generation.
 * Interview questions are now selected from a predefined list in the UI
 * to avoid API rate limits during development.
 */
